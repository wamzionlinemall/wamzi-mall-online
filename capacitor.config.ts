import type { CapacitorConfig } from '@capacitor/cli'
const config: CapacitorConfig = {
  appId: 'tz.wamzi.mall',
  appName: 'WAMZI MALL ONLINE',
  webDir: 'dist',
  server: { androidScheme: 'https' }
}
export default config